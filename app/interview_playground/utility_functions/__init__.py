"""
Utility functions package for interview playground.

Previously re-exported a vendored Excalidraw-to-Mermaid parser. The design canvas
now serializes itself in the browser, so nothing here translates diagrams any more.
"""
